spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: jimenezrogelio151@gmail.com
    password: rkgpvxsjpukmtghp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    database: h2
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: true
    show-sql: false
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      
server:
  port: 8080